schema: 'http://localhost:8000/graphql/'
generates:
  ./src/gql/:
    preset: 'client'
    plugins: []
    config:
      useTypeImports: true
documents: ['**/*.vue', '**/*.ts']
ignoreNoDocuments: true
